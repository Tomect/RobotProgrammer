using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace RobotProgrammer
{
    class Open
    {
        public Boolean OpenProgram(string filename)
        {
            string XML = string.Empty;
            // Check File exists

            // Open File

            // Pass the file to the XML Parser
            return XML_Parser(XML);

        }

        private Boolean XML_Parser(string XML)
        {
            return true;
        }
    }
}
